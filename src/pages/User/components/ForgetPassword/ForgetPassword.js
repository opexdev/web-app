import React from 'react'
import classes from '../../User.module.css'

const ForgetPassword = () => {
    return (
        <div className={`${classes.content} card-border column jc-around ai-center py-2`}>

        </div>
    );
};

export default ForgetPassword;