import React from "react";
import classes from "./../../TheOrder.module.css";
import {useTranslation} from "react-i18next";



const TheSellOrder = () => {

    const {t} = useTranslation();


    return (
        <div className={`column jc-between ${classes.content}`}>

        </div>
    );
};

export default TheSellOrder;
